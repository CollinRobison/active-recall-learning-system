# OpenCode adapter

Reference the shared `protocol/`, `skills/`, and `prompts/` files from the OpenCode instruction configuration. The adapter maps available filesystem, search, command, and confirmation operations only; all learning behavior remains in the portable core.
