# Copilot CLI adapter

Make the shared Markdown package discoverable in Copilot CLI instructions. Provide the active workspace path and map local reads, writes, search, commands, and explicit confirmation. Do not assume persistent conversation memory or a hosted database.
