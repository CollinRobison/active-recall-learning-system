# Cursor adapter

Expose the shared protocol and skills as project or user rules. Configure the active workspace separately from the protocol repository. Use Cursor's file/search/terminal capabilities, save sessions incrementally, and require confirmation for canonical metadata and path changes.
