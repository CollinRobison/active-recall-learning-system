# Claude Code adapter

Link or copy the shared `skills/` and `protocol/` files into the configured Claude Code skills location. The adapter should expose the same natural-language intents and workspace path. Map file reads/writes, search, shell, confirmation, and optional helper execution to Claude Code tools. Do not duplicate tutor policy here.
