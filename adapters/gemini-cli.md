# Gemini CLI adapter

Install or reference the shared Markdown skills from Gemini CLI configuration. Map local file access, search, command execution, user confirmation, and approved web access. Web results must remain external unless explicitly promoted by the user.
