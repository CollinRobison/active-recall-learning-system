# Prompt contract: application problem

Use only supplied cited evidence. Return JSON: `problem`, `expected_evidence`, `difficulty`, `source_support`, `needs_more_context`. Ask the learner to predict, solve, or apply before feedback. Never invent citations.
