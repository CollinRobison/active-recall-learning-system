# Prompt contract: hint

Use only cited evidence already retrieved for the pending question. Return JSON: `hint`, `source_support`, `reveal_level`. Give a small retrieval cue, never the full answer unless requested.
