# Prompt contract: topic/path classification

Use supplied source metadata only. Return JSON: `suggested_topics`, `suggested_paths`, `prerequisites`, `confidence`, `evidence`. Suggestions require user confirmation before modifying canonical catalog records.
